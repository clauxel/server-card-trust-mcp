{
  "type": "module",
  "private": true,
  "scripts": {
    "build:hotwords": "node scripts/build-hotwords.mjs",
    "deploy:hotwords": "npm run build:hotwords && npx wrangler deploy -c wrangler.hotwords.toml"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  }
}
